# Job Seeker Bot — Product & Engineering Roadmap

> نسخه تجمیعی — ۱۴۰۵/۰۷/۰۱ (2026-09-23)  
> محدوده محصول: **Telegram-first**؛ همه قابلیت‌های کاربر از طریق ربات تلگرام (پیام، دکمه، فایل و در صورت نیاز صوت) قابل استفاده‌اند. لینک درخواست استخدام ممکن است به وب‌سایت کارفرما هدایت کند. ابزارهای داخلی استقرار و مانیتورینگ لازم نیست داخل تلگرام باشند.

## راهنمای وضعیت

- **انجام‌شده (گزارش‌شده):** در گفت‌وگو پیاده‌سازی یا اجرای آن تأیید شده؛ ممیزی آخرین کد انجام نشده است.
- **نیازمند تأیید/تکمیل:** بخشی طراحی یا پیاده‌سازی شده، اما رفتار نهایی Production یا پوشش تست آن تأیید نشده است.
- **برنامه‌ریزی‌شده:** در Backlog قرار دارد و انجام‌شده تلقی نمی‌شود.

## وضعیت موجود — Baseline

### محصول و داده — انجام‌شده (گزارش‌شده)

- [x] ربات grammY، `StartHandler`، `JobHandler`، `JobFilterHandler` و `JobRenderer`.
- [x] نمایش آگهی‌ها، صفحه‌بندی Inline Keyboard، فیلتر نوع قرارداد و پاک‌کردن فیلتر.
- [x] Session اولیه برای فیلترها؛ **ذخیره پایدار Session هنوز جزو کارهای آینده است**.
- [x] Crawler سایت Jobinja با Playwright، معماری Provider/Parser قابل توسعه، Factory Method و Strategy.
- [x] `JobRepository` و `JobService`، مدل‌های `Job`، `Company` و `Location`، Prisma/PostgreSQL و یکتایی URL.
- [x] Scheduler روزانه ساعت ۱۰ تهران، تأخیر تصادفی تا ۱۰ دقیقه و جلوگیری از هم‌پوشانی در همان پردازش.

### زیرساخت — انجام‌شده (گزارش‌شده)

- [x] Dockerfile چندمرحله‌ای Node.js 22؛ سرویس‌های مستقل Bot، Crawler و PostgreSQL 14 در Docker Compose.
- [x] Volume دائمی PostgreSQL، GHCR، انتشار Image و Deploy دستی نسخه انتخابی با GitHub Actions و SSH.
- [x] انتقال Compose و `.env.production`، اعتبارسنجی Compose، اجرای Prisma Migration و راه‌اندازی سرویس‌ها.
- [x] اجرای موفق Deploy، Healthy شدن سرویس‌ها و شروع Crawl داخل Docker.
- [x] Health Check اولیه سرویس‌ها؛ **پایش زنده بودن واقعی Polling و اجرای روزانه هنوز باید تکمیل شود**.

### نیازمند تأیید یا تکمیل

- [x] ثبت کامل `CrawlerExecution` شامل `RUNNING`، `SUCCESS`، `FAILED`، زمان‌ها و خطا در اجرای واقعی.
- [x] مدیریت اجراهای ناقص و بازیابی Crawler بعد از Restart
- [x] `getLatestExecution` و منطق Health Check اجرای روزانه در آخرین Image تولیدی.
- [ ] استانداردسازی عنوان Squash Merge/PR برای Semantic Release و تست انتشار یک نسخه جدید.
- [ ] پوشش واقعی تست‌های خودکار و تأیید اجرای کامل چرخه Merge → Release → Deploy.
- [ ] Providerهای دیگر: در آخرین Bootstrap بررسی‌شده فقط `JobInJaCreator` فعال بود.

## فاز ۱ — Production Stability & Security

**هدف:** انتشار قابل اتکا، حفاظت از داده‌ها و امکان تشخیص و بازیابی خطا پیش از توسعه گسترده محصول.

- [ ] استانداردسازی Conventional Commit در عنوان PR و Squash Merge؛ رفع انتشارهای ازدست‌رفته و تست Semantic Release.
- [ ] تست سرتاسری CI/CD: کیفیت کد، Build، Release، GHCR، Deploy، Migration و Health Check.
- [ ] بکاپ خودکار PostgreSQL، نسخه خارج از VPS، سیاست نگهداری و **تست Restore**.
- [ ] Rollback نسخه Bot/Crawler در Deploy ناموفق؛ بررسی سازگاری و برنامه بازیابی Migration (بازگردانی Image لزوماً Schema را برنمی‌گرداند).
- [ ] تکمیل CrawlerExecution و تشخیص اجرای ناموفق، انجام‌نشده، طولانی یا `RUNNING` قدیمی؛ هشدار عملیاتی.
- [ ] بررسی واقعی زنده بودن Bot/Polling و پاسخ‌گویی برنامه؛ تفکیک سلامت Container از موفقیت Crawl.
- [ ] امنیت SSH Host Key، دسترسی محدود Deploy، مدیریت Secrets و جلوگیری از چاپ اطلاعات حساس در Log.
- [ ] Log Rotation، پایش دیسک، محدودیت RAM/CPU به‌ویژه Chromium و پاک‌سازی امن Imageهای قدیمی.
- [ ] تست Unit/Integration برای Repository، Service، Scheduler، Parser، Handler و مسیرهای خطا.
- [ ] سیاست اولیه حریم خصوصی، محدودیت دسترسی به داده‌های کاربران و حداقل‌سازی داده‌های حساس در Log.

**معیار اتمام:** نسخه جدید از PR تا Production منتشر و در صورت شکست بازیابی شود؛ بکاپ واقعاً قابل Restore باشد و خرابی سرویس/اجرای روزانه قابل تشخیص باشد.

## فاز ۲ — Smart & Reliable Crawling

**هدف:** آگهی‌های جدید را سریع‌تر، با درخواست کمتر و اطلاعات کامل‌تر جمع‌آوری کنیم.

- [ ] **Incremental Crawling:** مرتب‌سازی آگهی‌ها بر اساس تاریخ انتشار `DESC` در صورت پشتیبانی منبع؛ پردازش آگهی‌های امروز و دیروز؛ توقف زودهنگام فقط وقتی ترتیب زمانی قابل اعتماد است.
- [ ] ثبت آخرین Crawl موفق برای هر Provider و گسترش پنجره زمانی در صورت از دست رفتن چند روز؛ مدیریت تاریخ نامشخص، آگهی تمدیدشده و منطقه زمانی.
- [ ] **Full/Initial Crawl** مستقل برای منبع جدید یا بازیابی داده‌ها، قابل اجرای دستی و کنترل‌شده.
- [ ] **Job Details Crawling:** بازدید صفحه اختصاصی آگهی جدید و استخراج شرح شغل، مسئولیت‌ها، مهارت‌ها، سطح تجربه، نوع قرارداد، حضور/Remote، حقوق و مزایا، موقعیت، لینک درخواست، Visa Sponsorship و Relocation؛ فیلدهای ناموجود اختیاری باشند.
- [ ] توسعه مدل Prisma، Migration، DTO/Interface و Parser اختصاصی هر Provider برای اطلاعات تکمیلی؛ ثبت منبع و زمان استخراج.
- [ ] بررسی وجود آگهی قبل از دریافت جزئیات؛ به‌روزرسانی کنترل‌شده آگهی‌های تغییرکرده.
- [ ] صف پایدار پردازش جزئیات آگهی (در صورت نیاز، ابتدا PostgreSQL)، Worker مستقل، Retry/Backoff، Timeout و کنترل هم‌زمانی و نرخ درخواست.
- [ ] تضمین آزادسازی Browser در `finally`، بازیابی عملیات ناقص و جلوگیری از اجرای هم‌زمان بین چند نمونه با قفل مناسب.
- [ ] افزودن Greenhouse و سپس سایر منابع از جمله LinkedIn در صورت وجود روش دسترسی مجاز و قابل اتکا.
- [ ] شناسایی آگهی‌های یکسان میان Providerها؛ حفظ لینک منابع و جلوگیری از اعلان تکراری.
- [ ] تشخیص آگهی بسته/منقضی/تمدیدشده و تغییرات مهم آن؛ بازبینی دوره‌ای متناسب با محدودیت منابع.
- [ ] کنترل کیفیت داده، لینک‌های خراب، نشانه‌های آگهی مشکوک و امکان گزارش توسط کاربر.
- [ ] Observability هر Provider: تعداد بررسی‌شده/جدید/تکراری، جزئیات موفق/ناموفق، مدت، درخواست‌ها، آخرین صفحه و علت توقف؛ هشدار برای خروجی غیرعادی حتی با وضعیت `SUCCESS`.
- [ ] سیاست آرشیو و نگهداری آگهی‌ها بدون حذف داده‌های موردنیاز Bookmark و Application Tracker.

**معیار اتمام:** Crawl روزانه در حالت عادی محدود و قابل بازیابی است؛ جزئیات آگهی‌های جدید ذخیره می‌شود؛ خطاهای هر Provider و داده‌های ناقص قابل ردیابی‌اند.

## فاز ۳ — Telegram UX, Job Discovery & Search

**هدف:** یافتن و بررسی فرصت‌ها در خود تلگرام سریع و قابل فهم باشد.

- [ ] طراحی منوی اصلی، زیرمنوها، Back/Cancel/Home، ویرایش پیام به‌جای ارسال انبوه، مدیریت Callback منقضی و خطا.
- [ ] پشتیبانی فارسی/انگلیسی، نمایش RTL/LTR، صفحه‌بندی و تقسیم پیام‌های طولانی مطابق محدودیت تلگرام.
- [ ] فیلترهای عنوان، مهارت، شرکت، کشور/شهر، نوع قرارداد، سطح تجربه، حقوق، تاریخ، Provider، Remote/Hybrid/On-site و Sponsorship.
- [ ] جست‌وجوی متنی و ترکیب آن با فیلترها؛ جست‌وجوی معنایی/زبان طبیعی در مرحله پیشرفته پس از آماده شدن داده‌ها.
- [ ] نمایش جزئیات کامل آگهی در چند پیام یا فایل و دکمه‌های ذخیره، اشتراک‌گذاری، مشاهده شرکت و Apply.
- [ ] مرتب‌سازی نتایج بر اساس تازگی، حقوق در صورت قابل مقایسه بودن و بعداً تطبیق با پروفایل.
- [ ] Bookmark و فهرست فرصت‌های ذخیره‌شده؛ وضعیت دیده‌شده/ندیده، شمار فرصت‌های جدید، Hide/Unhide.
- [ ] لینک اختصاصی آگهی با Telegram Deep Link و دکمه Share.
- [ ] پروفایل شرکت در پیام‌های ربات: اطلاعات مستند، آگهی‌های فعال، وب‌سایت و امکان Follow.
- [ ] ترجمه و خلاصه‌سازی آگهی با حفظ لینک و متن اصلی.
- [ ] Job Market Insights مبتنی بر **داده‌های خود ربات**: روند آگهی‌ها، مهارت‌های پرتکرار، توزیع مکان و نوع همکاری.

**معیار اتمام:** کاربر بتواند بدون داشبورد وب آگهی را پیدا کند، جزئیاتش را بخواند، ذخیره/مخفی/اشتراک‌گذاری کند و نتایج را با فیلترهای ترکیبی مرور کند.

## فاز ۴ — User Profiles, Personalization & Alerts

**هدف:** ربات فرصت‌های مناسب را بدون جست‌وجوی روزانه به کاربر برساند.

- [ ] Onboarding قابل ردکردن: عنوان شغلی، تجربه، مهارت، مکان، نوع همکاری، Sponsorship و ترجیحات اعلان.
- [ ] مدل User در PostgreSQL، تنظیمات زبان/منطقه زمانی، ترجیحات و حذف/ویرایش پروفایل.
- [ ] Session پایدار (مثلاً PostgreSQL)، انقضا، مدیریت نسخه Session و بازیابی پس از Deploy.
- [ ] ذخیره چند مجموعه فیلتر و انتخاب فیلتر پیش‌فرض.
- [ ] Job Alerts فوری/روزانه/هفتگی با تنظیم ساعت، سقف تعداد پیام و Pause/Resume.
- [ ] ارسال اعلان آگهی جدید یا شرکت Followشده، با جلوگیری از تکرار برای هر کاربر و هر آگهی.
- [ ] صف پایدار اعلان، وضعیت ارسال، Retry/Backoff، رعایت Rate Limit تلگرام، مدیریت Block شدن Bot و خطاهای دائمی.
- [ ] نمایش دلیل تطبیق اعلان با فیلتر و امکان اصلاح سریع فیلتر از همان پیام.

**معیار اتمام:** پس از تنظیم اولیه، کاربر اعلان مرتبط را در زمان دلخواه می‌گیرد و Restart/Deploy باعث گم شدن بی‌صدای اعلان یا Session نمی‌شود.

## فاز ۵ — AI Career Assistant & International Jobs

**هدف:** تحلیل قابل توضیح رزومه و شرایط آگهی در قالب پیام و فایل داخل ربات.

- [ ] دریافت PDF/DOCX رزومه در تلگرام، استخراج اطلاعات و تأیید/اصلاح توسط کاربر.
- [ ] نگهداری چند رزومه و نسخه، انتخاب رزومه پیش‌فرض و اتصال نسخه مشخص به هر درخواست شغلی.
- [ ] AI Job Matching بر اساس مهارت، تجربه و ترجیحات؛ نمایش دلایل تطبیق و موارد نامشخص، بدون ادعای احتمال استخدام.
- [ ] Resume Analyzer و بررسی ساختار/خوانایی ATS با توضیح محدودیت‌ها.
- [ ] تولید رزومه متناسب با آگهی بر پایه سوابق واقعی و خروجی PDF/DOCX در تلگرام؛ تأیید کاربر قبل از استفاده.
- [ ] تولید و ویرایش Cover Letter اختصاصی و ارسال متن/فایل.
- [ ] استخراج مستند Visa Sponsorship، Relocation، محدودیت جغرافیایی، Work Authorization، Remote Worldwide و شرایط استخدام بین‌المللی؛ تفکیک صریحِ مثبت/منفی/نامشخص.
- [ ] Salary Insights و مقایسه داده‌های حقوق با ارز، تاریخ، منبع و تفکیک ناخالص/خالص در صورت وجود.
- [ ] کنترل هزینه و مصرف AI، محدودیت درخواست، Timeout/Retry و جلوگیری از پردازش تکراری.
- [ ] رضایت برای پردازش رزومه، سیاست نگهداری فایل، دسترسی کاربر به داده‌های خود، خروجی و حذف اطلاعات شخصی از طریق ربات.

**معیار اتمام:** کاربر بتواند رزومه‌اش را بفرستد، آگهی‌های مرتبط و شرایط بین‌المللی را بررسی کند و اسناد شخصی‌سازی‌شده را داخل تلگرام دریافت کند.

## فاز ۶ — Application Management & Interview

**هدف:** مدیریت مسیر پس از کشف فرصت تا نتیجه درخواست، بدون داشبورد وب.

- [ ] Application Tracker با وضعیت‌های Saved، Applied، Interview، Offer، Rejected و Withdrawn و تاریخچه تغییر وضعیت.
- [ ] ثبت تاریخ ارسال، نسخه رزومه استفاده‌شده، یادداشت‌ها، اطلاعات Recruiter و لینک درخواست.
- [ ] یادآوری Follow-up، زمان مصاحبه و کارهای بعدی با امکان ویرایش/لغو در تلگرام.
- [ ] AI Mock Interview متنی؛ صوتی در صورت مناسب بودن هزینه و تجربه کاربری، همراه با بازخورد و سؤال بعدی.
- [ ] تولید سؤالات فنی، رفتاری و متناسب با شرح شغل و برنامه تمرین.
- [ ] مقایسه مستند Offerها بر اساس حقوق، مزایا، قرارداد، حضور و Sponsorship بدون تصمیم‌گیری به‌جای کاربر.
- [ ] گزارش شخصی تعداد درخواست‌ها، مصاحبه‌ها، Offerها و پیگیری‌های باز در پیام یا فایل.

**معیار اتمام:** کاربر وضعیت هر درخواست و کار بعدی را داخل ربات ثبت و پیگیری کند.

## فاز ۷ — Monetization, Support & Growth

**هدف:** آزمایش مدل درآمدی و رشد، بدون ساخت وب‌اپلیکیشن یا پنل کارفرما.

- [ ] تعریف پلن Free/Pro و سهمیه‌های شفاف برای Alerts، AI و ابزارهای پیشرفته.
- [ ] مدیریت اشتراک، انقضا، تمدید، ارتقا و مصرف از منوی ربات.
- [ ] روش پرداخت سازگار با قواعد تلگرام و بازار هدف؛ ثبت تراکنش، رسیدگی به خطا و فعال‌سازی/لغو اشتراک.
- [ ] Referral Program با لینک دعوت، ثبت دعوت معتبر و کنترل سوءاستفاده.
- [ ] پشتیبانی، ثبت بازخورد و گزارش آگهی مشکوک از داخل ربات.
- [ ] آمار استفاده از محصول و قیف تبدیل با حداقل‌سازی داده‌های شخصی.
- [ ] تعیین سیاست نگهداری داده‌ها، پاک‌سازی Session/Log/فایل‌های منقضی و حفظ سوابق ضروری کاربر.

**معیار اتمام:** کاربر بتواند اشتراک را در ربات بخرد و مدیریت کند و تیم بتواند هزینه، کیفیت و استفاده از محصول را پایش کند.

---

## ترتیب تحویل و وابستگی‌های مهم

1. **ابتدا فاز ۱:** Release، Backup/Restore، Rollback و پایش اولیه را تثبیت کن.
2. **سپس فاز ۲:** ابتدا Incremental + Full/Recovery؛ بعد Job Details + Schema؛ صف پردازش را وقتی حجم/نیاز به Retry مستقل ایجاب کرد اضافه کن.
3. **بعد فاز ۳:** UX، جزئیات، جست‌وجو و فیلترهای پیشرفته را بر داده‌های غنی‌شده بساز.
4. **فاز ۴:** پروفایل و Session پایدار، سپس فیلترهای ذخیره‌شده و ارسال مطمئن اعلان.
5. **فاز ۵:** رزومه و AI پس از آماده شدن داده و سیاست حریم خصوصی.
6. **فاز ۶:** Tracker و ابزار مصاحبه را بر پروفایل و رزومه کاربر متصل کن.
7. **فاز ۷:** پرداخت و پلن‌ها را پس از سنجش استفاده و هزینه واقعی آزمایش کن.

> فازها ترتیب غالب هستند، نه مانع انتشار تدریجی: مثلاً UX پایه و Onboarding ساده می‌توانند زودتر تحویل شوند؛ Queue، جست‌وجوی معنایی و مصاحبه صوتی را بدون نیاز واقعی زودهنگام پیچیده نکن.

## خارج از محدوده فعلی

- وب‌اپلیکیشن یا داشبورد مستقل کاریابی و اپ موبایل/دسکتاپ مستقل.
- پنل وب کارفرما و ATS اختصاصی کارفرما.
- شبکه اجتماعی کاریابی یا Referral Network بین کاربران (با Referral Program دعوت به ربات تفاوت دارد).
- ارسال خودکار درخواست استخدام به‌جای کاربر بدون طراحی و تأیید جداگانه.

## قدم بعدی پیشنهادی

تأیید آخرین وضعیت `CrawlerExecution` و Semantic Release در Repository، سپس پیاده‌سازی بکاپ قابل Restore و Rollback؛ بعد وارد Incremental Crawling و Job Details Crawling شویم.
